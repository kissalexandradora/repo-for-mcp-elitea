{
  "name": "epam-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for EPAM website",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.7.2"
  }
}
